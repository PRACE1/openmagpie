# API origin (Django). The Next.js app fetches /v1/auth/* etc. against this base.
NEXT_PUBLIC_API_URL=http://localhost:8000
# API version path prefix — must match Django's API_VERSION_PREFIX setting.
NEXT_PUBLIC_API_VERSION=v1
